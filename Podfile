# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'iOS 12 Notifications' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for iOS 12 Notifications

end

target 'Notification Content App Extension' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
	pod 'Alamofire', '~> 5.0.0-rc.2'
	pod ‘SQLite.swift’, :git => ‘https://github.com/stephencelis/SQLite.swift.git’, :branch => ‘master’
	pod 'ReachabilitySwift'
  # Pods for Notification Content App Extension

end
